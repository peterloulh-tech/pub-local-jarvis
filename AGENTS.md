# Development Discipline

- 每次只处理一个边界明确、可验证的任务。
- 修改前必须从真实入口沿引用、调用和数据流追踪完整链路，不得根据文件名猜测。
- 不得只修改界面而遗漏后端、原生 Worker、持久化或实际运行配置中的业务逻辑。
- 未经用户明确确认，不得修改 C++ 原生层、CUDA 构建、模型格式/布局或 Electron-Python-C++ 通信协议。
- 不得进行与当前任务无关的重构、格式化、依赖升级或文件移动。
- 不得对全仓库的 Jarvis/AIJarvis/JARVIS 相关字符串做批量全局替换；展示品牌与内部兼容标识必须分别评估。
- 必须保留根目录 MIT `LICENSE`、`THIRD_PARTY_NOTICES.md` 及第三方许可证和声明；发布产物也必须核验保留情况。
- 存在与当前修改直接相关且可执行的测试时，必须运行并报告实际命令和结果；纯文档或纯分析任务不强制运行无关业务测试，无法运行的相关测试必须明确说明原因。
- 无法在 Mac 验证的 Windows、CUDA、NVIDIA、安装包、全屏窗口和真实游戏功能必须明确标记为未实机验证。
- GitHub Actions 的 Windows 编译和自动测试只能证明对应云端步骤通过；安装、全屏游戏、窗口行为、采集和 NVIDIA/CUDA 运行效果仍必须在真实 Windows 设备集中验证并记录结果。
- 修改模块边界、运行模式、跨进程数据流、构建验证方式或产品状态后，必须同步更新相关代码库地图、功能地图、产品需求或开发计划，并明确区分当前实现、待云端验证和未来规划。
- 同一任务中只有一个 Agent 可以写代码；其他 Agent 只能进行只读分析或审查，避免并发覆盖。
- 只有修改超出用户明确授权的任务边界时，才必须停止并等待确认；在已授权业务范围内，为追踪和完成完整数据流而涉及相邻模块，不视为自动越界，但仍然禁止无关重构。
- 适用测试失败时不得声称任务完成；相关测试无法运行或必要平台验证缺失时，必须说明原因、未验证范围和剩余风险，不得声称已经完成对应验证。
- 每个 AI Jarvis 任务开始前，必须先确认当前处于总需求规划的哪个阶段、按总规划顺序下一项是什么、本次修改是否需要同步项目基础文档，以及当前方案是否满足节省总额度原则。
- 所有开发决策以节省总额度、减少返工和降低整体风险为目标，默认采用“最小正确闭环”和“最小充分改动”；局部问题不扩大，核心问题不绕开。
- 实现功能需要涉及几层，就允许修改完成真实闭环所必需的几层；不得为了少改文件而遗漏实际生效层或形成假闭环。
- 默认只定位和读取当前任务直接相关的文件，禁止无必要的全仓库审计。
- 优先使用失败测试证明问题，再做最小充分修改和针对性验证；除任务明确需要外，不默认运行完整测试、下载 CUDA 或执行耗时构建。
- Codex 可以按任务要求创建本地分支和本地提交，但不得执行 push、认证、创建 Pull Request 或合并；推送、创建 PR 和合并全部由用户手动完成。
- Mac 测试不能替代 Windows、DXGI、WASAPI、Native、CUDA 和 NVIDIA 实机验证；未完成必要实测的功能必须标记为“代码已实现，待 Windows 或实机验证”。
